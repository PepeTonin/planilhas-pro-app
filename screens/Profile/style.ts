import { StyleSheet } from "react-native";
import { Colors } from "../../styles/Colors";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: Colors.primaryBlack,
    paddingHorizontal: 20,
  },
  text: { color: Colors.whiteF5 },
});
